#
# global filter
#
# *:          # all action target.
# foo:        # foo controller target.
# foo.index:  # foo.index action target.
#

*:
    ExecutionTime:
    Raikiri:
        dicon:
            - App/Config/App/samurai.dicon
    Onikiri:
    View:

